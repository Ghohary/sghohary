{
  "name": "ghohary-stripe-payments",
  "version": "1.0.0",
  "description": "Stripe payment processing server for GHOHARY Haute Couture",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "dev": "nodemon server.js",
    "test": "echo \"Error: no test specified\" && exit 1",
    "deploy:preview": "npm run verify:no-blob && vercel",
    "deploy:prod": "npm run verify:no-blob && bash ./deploy-main-domain.sh",
    "deploy:prod:plain": "npm run verify:no-blob && vercel --prod --yes",
    "verify:no-blob": "if rg -n --hidden -i --glob '!node_modules' --glob '!.git' --glob '!package.json' --glob '!**/*.md' --glob '!.github/workflows/no-blob-policy.yml' \"(blob\\.vercel-storage\\.com|@vercel/blob|vercel-blob|vercel\\s+blob|BLOB_PRIVATE_READ_WRITE_TOKEN|BLOB_READ_WRITE_TOKEN|\\bBLOB_[A-Z_]*|\\bBLOB_\\w*=[^ ]+)\" .; then echo \"Blob references detected. Remove these before deployment.\" && exit 1; else echo \"No Blob references found.\"; fi",
    "import:catalog": "node scripts/import-catalog-products.mjs"
  },
  "keywords": [
    "stripe",
    "payments",
    "ecommerce",
    "haute-couture"
  ],
  "author": "GHOHARY",
  "license": "MIT",
  "dependencies": {
    "@aws-sdk/client-s3": "^3.1000.0",
    "cors": "^2.8.5",
    "dotenv": "^16.0.3",
    "express": "^4.22.1",
    "redis": "^4.6.13",
    "sharp": "^0.34.4",
    "stripe": "^14.0.0"
  },
  "devDependencies": {
    "nodemon": "^3.0.1"
  },
  "engines": {
    "node": "20.x"
  }
}
